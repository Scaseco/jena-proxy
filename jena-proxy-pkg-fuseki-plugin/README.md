
Build the self contained fuseki mod jar from source: `mvn -Pbundle clean package`.
Build the self contained fuseki mod jar from this pom.xml alone (requires a prior release): `mvn -Punpack-own-classes,bundle clean package`.

